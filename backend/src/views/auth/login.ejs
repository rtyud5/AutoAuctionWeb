<% layout("layouts/main") %>

<section class="min-h-[70vh] flex items-center justify-center py-10">
  <div class="max-w-md w-full bg-white/90 shadow-xl shadow-slate-200/60 rounded-2xl border border-slate-100 p-6 space-y-4">
    <header class="space-y-1">
      <h1 class="text-xl font-semibold text-slate-900">Đăng nhập tài khoản</h1>
      <p class="text-sm text-slate-500">
        Sử dụng email và mật khẩu đã đăng ký để tham gia đấu giá.
      </p>
    </header>

    <% if (error) { %>
      <div class="text-sm px-3 py-2 rounded-lg border border-rose-200 bg-rose-50 text-rose-700">
        <%= error %>
      </div>
    <% } %>

    <% if (typeof adminError !== "undefined" && adminError) { %>
      <div class="text-sm px-3 py-2 rounded-lg border border-amber-200 bg-amber-50 text-amber-700">
        <%= adminError %>
      </div>
    <% } %>

    <form action="/auth/login" method="POST" class="space-y-4">
      <div class="space-y-1.5">
        <label for="email" class="block text-sm font-medium text-slate-700">
          Email
        </label>
        <input
          id="email"
          type="email"
          name="email"
          required
          class="block w-full rounded-xl border border-slate-200 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-teal-400 focus:border-teal-400 bg-white"
          placeholder="you@example.com"
        />
      </div>

      <div class="space-y-1.5">
        <label for="password" class="block text-sm font-medium text-slate-700">
          Mật khẩu
        </label>
        <input
          id="password"
          type="password"
          name="password"
          required
          class="block w-full rounded-xl border border-slate-200 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-teal-400 focus:border-teal-400 bg-white"
          placeholder="••••••••"
        />
      </div>

      <div class="flex items-center justify-between text-xs text-slate-500">
        <div class="flex items-center gap-2">
          <input id="remember" type="checkbox" class="rounded border-slate-300 text-teal-500 focus:ring-teal-400" />
          <label for="remember">Ghi nhớ đăng nhập</label>
        </div>
        <a href="#" class="hover:text-teal-600">Quên mật khẩu?</a>
      </div>

      <button
        type="submit"
        class="w-full inline-flex items-center justify-center rounded-xl bg-teal-500 hover:bg-teal-600 text-white text-sm font-medium py-2.5 transition"
      >
        Đăng nhập
      </button>
    </form>

    <div class="pt-2 border-t border-dashed border-slate-200 space-y-2">
      <p class="text-xs text-slate-500 text-center">
        Chưa có tài khoản?
        <a href="/register" class="text-teal-600 font-medium hover:underline">Đăng ký ngay</a>
      </p>

      <div class="flex items-center gap-2 text-[11px] text-slate-500 justify-center">
        <span class="h-px w-10 bg-slate-200"></span>
        <span>Hoặc</span>
        <span class="h-px w-10 bg-slate-200"></span>
      </div>

      <div class="flex justify-center">
        <button
          type="button"
          id="admin-login-trigger"
          class="text-xs font-medium text-slate-600 hover:text-teal-600 underline underline-offset-2"
        >
          Đăng nhập với quyền Admin
        </button>
      </div>
    </div>
  </div>
</section>

<!-- Admin login modal -->
<div
  id="admin-login-modal"
  class="fixed inset-0 z-40 hidden items-center justify-center bg-slate-900/60 backdrop-blur-sm"
>
  <div class="w-full max-w-sm bg-white rounded-2xl shadow-xl border border-slate-200 p-5 space-y-4">
    <header class="space-y-1">
      <h2 class="text-base font-semibold text-slate-900">Đăng nhập Admin</h2>
      <p class="text-xs text-slate-500">
        Nhập Admin key để truy cập khu vực quản trị. Luồng này độc lập với tài khoản người dùng.
      </p>
    </header>

    <form id="admin-login-form" method="POST" action="/auth/admin-login" class="space-y-4">
      <div class="space-y-1.5">
        <label for="adminKey" class="block text-xs font-medium text-slate-700">
          Admin key
        </label>
        <input
          id="adminKey"
          type="password"
          name="adminKey"
          required
          class="block w-full rounded-xl border border-slate-200 px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-teal-400 focus:border-teal-400 bg-white"
          placeholder="Nhập admin key (mặc định: admin123)"
        />
      </div>

      <div class="flex items-center justify-end gap-2 pt-2">
        <button
          type="button"
          id="admin-login-cancel"
          class="px-3 py-1.5 rounded-xl text-xs font-medium text-slate-600 hover:bg-slate-100"
        >
          Hủy
        </button>
        <button
          type="submit"
          class="px-3 py-1.5 rounded-xl text-xs font-medium bg-teal-500 text-white hover:bg-teal-600"
        >
          Vào Admin
        </button>
      </div>
    </form>
  </div>
</div>

<script>
  (function () {
    const trigger = document.getElementById("admin-login-trigger");
    const modal = document.getElementById("admin-login-modal");
    const cancelBtn = document.getElementById("admin-login-cancel");

    if (!trigger || !modal) return;

    const openModal = () => {
      modal.classList.remove("hidden");
      modal.classList.add("flex");
      const input = document.getElementById("adminKey");
      if (input) input.focus();
    };

    const closeModal = () => {
      modal.classList.add("hidden");
      modal.classList.remove("flex");
    };

    trigger.addEventListener("click", openModal);
    cancelBtn?.addEventListener("click", (e) => {
      e.preventDefault();
      closeModal();
    });

    modal.addEventListener("click", (e) => {
      if (e.target === modal) {
        closeModal();
      }
    });
  })();
</script>
